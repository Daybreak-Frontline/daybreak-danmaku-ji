# Default desktop / portable port is 54821.
# PORT=54821

# Override the data directory (SQLite + encrypted cookies). Default: ./data
# MUSICHE_DATA=./data

# Used to encrypt login cookies at rest. Change this for any shared machine.
# MUSICHE_SESSION_SECRET=change-me
